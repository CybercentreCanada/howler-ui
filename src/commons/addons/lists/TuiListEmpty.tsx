import { StackProps } from '@mui/material';
import AppListEmpty from 'commons/components/display/AppListEmpty';

export default function TuiListEmpty(props: StackProps) {
  return <AppListEmpty {...props} />;
}
